//de bai
var options = {
    size: {
        width: 100,
        height: 200,
    },
    items: ["Cake", "Donut"],
    extra: true
};
var width = options.size.width;
var height = options.size.height;
var item1 = options.items[0];
var item2 = options.items[1];
console.log(width, height, item1, item2);
